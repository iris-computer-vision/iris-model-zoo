# iris-model-zoo
A collection of real-world object detection models built with IRIS across domains like drone, weapons, and aircraft detection. Each model is trained on operational data, enabling fast evaluation and deployment of production-relevant models.
